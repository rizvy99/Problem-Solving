#include <iostream>
#include <cmath>

int main() {
    int a = 5;
    int b = 8;

    // Using abs() to get the absolute difference between a and b
    int absoluteDifference = abs(a - b);

    std::cout << "Absolute Difference: " << absoluteDifference << std::endl;

    return 0;
}
